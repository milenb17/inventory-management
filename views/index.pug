extends layout

block content
  h1= title
  p Welcome to #[em Inventory Management], a very basic Express website.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The grocery store has the following records:

    ul
      li #[strong Items:] !{data.itemCount}
      li #[strong Categories:] !{data.categoryCount}
